### Setting and Using Marks

- **Set a Mark**: Use `m` followed by a letter (e.g., `ma`).
- **Jump to a Mark**: Use `'` (to jump to the start of the line) or `` \` `` (to jump to the exact position).
- **Delete a Mark**: Use `dm` followed by the letter (e.g., `dma` deletes mark 'a').